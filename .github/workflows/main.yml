name: Generate Snake Contribution Graph

on:
  schedule:
    - cron: "0 0 * * *"  # Exécute tous les jours à minuit

  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du repo
        uses: actions/checkout@v3

      - name: Générer le graphe Snake
        uses: Platane/snk@master
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: github-contribution-grid-snake.svg

      - name: Commit et Push
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "github-actions@github.com"
          git add github-contribution-grid-snake.svg
          git commit -m "Mise à jour du graphe Snake" || exit 0
          git push https://TEMGOUAemmauelle:${{ secrets.PAT }}@github.com/${{ github.repository }}.git HEAD:main
